import { styled } from "@mui/material/styles";
import { Typography, Box, rgbToHex } from "@mui/material";


export const notFound = styled(Box)({
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    textAlign:'center',
    alignItems: 'center',
    color: '#686868',
    minHeight: '88vh',

})